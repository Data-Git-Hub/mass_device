# =========================================
# Python build / cache artifacts
# These are auto-generated and should never be committed.
# =========================================
__pycache__/
*.pyc
*.pyo
*.pyd

# Packaging/build outputs (created by setup/build tools)
*.egg-info/
.eggs/
build/
dist/

# Virtual environments (local-only Python env)
.venv/
venv/

# =========================================
# Environment & secrets
# Keep real secrets out of version control.
# Track ONLY the template `.env.example`.
# =========================================
.env
.env.*
!.env.example

# =========================================
# Editor/IDE settings
# Project-specific settings can be noisy; teammates’ editors may differ.
# (If you WANT to share VS Code settings, remove `.vscode/` from here.)
# =========================================
.vscode/
.code-workspace
# JetBrains (PyCharm) — safe to ignore if not used
.idea/

# =========================================
# OS-generated files
# Hidden files created by macOS/Windows file explorers.
# =========================================
.DS_Store
Thumbs.db
Desktop.ini

# =========================================
# Logs & runtime outputs
# Logs and temp files belong on the local machine, not in Git history.
# =========================================
logs/
*.log
*.tmp
*.bak

# =========================================
# Data artifacts
# Keep large/derived data out of the repo. Commit only small sample data if needed.
# =========================================
data/*.parquet
data/*.db
data/*.sqlite
data/generated/
# If you keep tiny demo CSVs, whitelist them explicitly, e.g.:
# !data/samples/*.csv

# =========================================
# Notebooks (optional)
# Uncomment if you don't want to track Jupyter checkpoints.
# =========================================
# .ipynb_checkpoints/

# =========================================
# OS/Tooling caches (optional)
# =========================================
# node_modules/
# coverage/
# .pytest_cache/
